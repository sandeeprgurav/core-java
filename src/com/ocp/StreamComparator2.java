package com.ocp;

import java.util.Comparator;
import java.util.stream.Stream;

public class StreamComparator2 {
public static void main(String[] args) {
/*	Stream<String> s = Stream.of("over the river", "through the woods", "to grandmother's house we go");
	s.filter(n -> n.startsWith("t"))
	.sorted(Comparator::reverseOrder) // reverseOrder requires two parameter but passed here only one, compile error
	.findFirst()
	.ifPresent(System.out::println);*/
}
}

//Ans: Non of the above